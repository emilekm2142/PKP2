using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Walizka : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void OnMouseUp()
    {
        Destroy	(this.gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
